#!/usr/bin/env node

/*
 * (MIT) This file is part of the fingerlog
 */
'use strict';
const app = require('../app');
const http = require('http');

const server = http.createServer(app);

const PORT = process.env.PORT || 3000;

server.listen(PORT, () => {
	console.log(`Running on : ${ PORT }`);
});
